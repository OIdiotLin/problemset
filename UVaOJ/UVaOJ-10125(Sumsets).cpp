#include <iostream>
#include <cstring>
#include <algorithm>
#include <cstdio>
#include <ctime>
#define MAXN 1010
#define INF (1<<28)
#define SpeedUp ios::sync_with_stdio(false)
#define TIMEUSED cout<<(double)clock()/(doule)CLOCKS_PER_SEC<<endl
//#define FILE
using namespace std;

int A[MAXN],d[MAXN*MAXN];

void init()
{
	memset(d,0,sizeof(d));
}

int main()
{
	SpeedUp;
	while()
	TIMEUSED;
	return 0;
}

